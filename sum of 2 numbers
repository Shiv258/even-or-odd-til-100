#include <stdio.h>

int main() {
    int a,b,s=0;
    printf("enter first number");
    scanf("%d",&a);
    printf("enter second number");
    scanf("%d",&b);
    s=a+b;
    printf("sum of %d and %d is %d",a,b,s);
    return 0;
}
